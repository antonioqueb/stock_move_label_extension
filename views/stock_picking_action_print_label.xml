<odoo>
    <record id="action_stock_picking_print_labels" model="ir.actions.server">
        <field name="name">Imprimir Etiquetas</field>
        <field name="model_id" ref="stock.model_stock_picking"/>
        <field name="state">code</field>
        <field name="code">
            action = env.ref('stock_move_label_extension.action_report_product_label_letter').report_action(model)
        </field>
    </record>
</odoo>
